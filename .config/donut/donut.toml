destination = '$HOME'
source = '$HOME/.local/share/donut'
editor = ["nvim"]
pager = ["delta"]
merge = ["nvim", "-d", "{{.Destination}}", "{{.Source}}"]
excludes = [
  ".gitignore",
  "scripts",
  "README.md",
  "LICENSE",
  "Brewfile",
  "app.md",
  "my.rayconfig",
]
